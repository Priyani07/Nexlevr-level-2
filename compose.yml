services:
  mongo:
    image: mongo:7.0
    command: ["mongod", "--replSet", "rs0", "--bind_ip_all"]
    ports:
      - "127.0.0.1:27017:27017"
    volumes:
      - shoplane-mongo:/data/db
    healthcheck:
      test: ["CMD-SHELL", "mongosh --quiet --eval 'try { rs.status() } catch (e) { rs.initiate({_id:\"rs0\",members:[{_id:0,host:\"localhost:27017\"}]}) }' && mongosh --quiet --eval 'if (!db.hello().isWritablePrimary) quit(1)'"]
      interval: 5s
      timeout: 10s
      retries: 20
volumes:
  shoplane-mongo:
